DIVERGENT BODIES — same fact, content differs; neither is a prefix.
Auto-merging these risks conflating intents, so pick ONE per cluster.

### Cluster 1
    Type wifiadd followed by the network name and password, then openwifi to connect.
    Type wifiadd followed by the network name and password, then openwifi to connect. Once connected, open the web dashboard from your phone or laptop using the device IP address.
    HardwareOne has built-in WiFi on the ESP32-S3. Type wifiadd followed by the network name and password, then openwifi to connect.
    WiFi connects the device to a router for internet access and the web dashboard. Type wifiadd followed by the network name and password, then openwifi to connect.
    Check that the network name and password are correct. Type wifiadd followed by the SSID and password, then openwifi to connect. Type wifistatus to see the current status.
    The command is wifiadd followed by the network name and password, then openwifi to connect. Once connected, open the web dashboard from your phone or laptop using the device IP address.

### Cluster 2
    Go to the Settings tab in the web UI and use the OTA section to upload a new firmware binary.
    Open the Settings tab in the web UI and use the OTA section to upload a new firmware binary. The device reboots automatically after update.
    Open the Settings tab in the web UI and use the OTA section to upload a new firmware binary. The device reboots automatically after the update. Settings are preserved.

### Cluster 3
    Type wifistatus to see the connection status, IP address, and signal strength.
    Type wifistatus to show the current connection status, IP address, and signal strength.
    wifistatus shows the connection status, IP address, and signal strength.
    Use wifistatus to check the connection status, IP address, and signal strength.
    wifistatus shows the current connection status, IP address, and signal strength.
    The command is wifistatus. It shows the current connection status, IP address, and signal strength.

### Cluster 4
    Try typing reboot to restart it. Check that the serial connection is active.
    Try typing reboot to restart it. Check that the serial or web connection is active. Type uptime to see if it recently rebooted.

### Cluster 5
    Type time to see the current device time, or ntpsync to sync from an NTP server.
    Run time to see the current device clock, or ntpsync to sync from an NTP server.

### Cluster 6
    Type IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.
    The syntax is IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.

### Cluster 7
    Type openthermal to start the MLX90640 thermal camera. The web UI shows a live 32 by 24 heatmap. Type closethermal to stop it.
    openthermal starts the MLX90640 thermal camera. The web UI shows a live 32 by 24 heatmap. closethermal stops it.
    Run openthermal to start the MLX90640 thermal camera. The web UI shows a live 32 by 24 heatmap. Run closethermal to stop it.
    Use openthermal to start the MLX90640 thermal camera. The web UI shows a live 32 by 24 heatmap. Use closethermal to stop it.

### Cluster 8
    Type uptime to see if it recently rebooted. Check debug logs on the SD card for crash information.
    Run uptime to check if it recently rebooted. Check debug logs on the SD card for crash information.

### Cluster 9
    Type SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.
    The command is SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.

### Cluster 10
    Type close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.
    The command is close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.

### Cluster 11
    Type filedelete followed by the file path, or use the delete button in the Files tab.
    The command is filedelete followed by the file path, or use the delete button in the Files tab.

### Cluster 12
    Type ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.
    The command is ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.

### Cluster 13
    Type open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.
    The command is open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.

### Cluster 14
    Type useradd followed by the username, password, and role.
    The command is useradd followed by the username, password, and role.

### Cluster 15
    Type settings to see current values, or use the Settings tab in the web UI. Settings are saved as JSON on internal storage.
    Use settings to see current values, or open the Settings tab in the web UI. Settings are saved as JSON on internal storage.
    The settings command shows current values, or use the Settings tab in the web UI. Settings are saved as JSON on internal storage.

### Cluster 16
    Type closewifi to disconnect from the current network.
    Type closewifi to turn off the WiFi radio and disconnect from the network.

### Cluster 17
    Type espnowsendfile followed by the peer MAC address and the file path.
    Yes, run espnowsendfile followed by the peer MAC address and the file path.

### Cluster 18
    Type espnowstats to see the connection status and the list of paired peers.
    espnowstats shows the connection status and the list of paired peers.

### Cluster 19
    Type i2cscan to scan the I2C bus. The device also scans automatically at startup and loads drivers for recognised sensors.
    i2cscan scans the I2C bus. The device also scans automatically at startup and loads drivers for recognised sensors.

### Cluster 20
    Type ledcolor followed by a color name or hex code. For example, ledcolor red or ledcolor FF00FF.
    ledcolor followed by a color name or hex code sets the LED. For example, ledcolor red or ledcolor FF00FF.

### Cluster 21
    Type mqttPublishIMU 1 or mqttPublishThermal 1 to enable automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.
    mqttPublishIMU 1 or mqttPublishThermal 1 enables automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.

### Cluster 22
    Type mqttSubscribeTopics followed by a comma-separated list of topics. Incoming messages appear in the serial log and can trigger automations.
    mqttSubscribeTopics followed by a comma-separated list of topics subscribes the device. Incoming messages appear in the serial log and can trigger automations.

### Cluster 23
    Type mqttstatus to see whether the device is connected to the broker and what topic prefix is being used.
    mqttstatus shows whether the device is connected to the broker and what topic prefix is being used.
    Use mqttstatus to check whether the device is connected to the broker and what topic prefix is being used.

### Cluster 24
    Type openrtc to start the DS3231 real-time clock, then rtcread to get the time. The RTC keeps time even when the device is powered off.
    openrtc starts the DS3231 real-time clock, then rtcread gets the time. The RTC keeps time even when the device is powered off.

### Cluster 25
    Type uptime to see how long the device has been running since the last reboot.
    uptime shows how long the device has been running since the last reboot.

### Cluster 26
    Type userdelete followed by the username to remove their account.
    userdelete followed by the username removes their account.

### Cluster 27
    Type userlist to see all user accounts and their roles.
    userlist shows all user accounts and their roles.

### Cluster 28
    Type wifiscan to see a list of nearby WiFi networks with signal strength.
    wifiscan shows a list of nearby WiFi networks with signal strength.

### Cluster 29
    Use IF followed by a condition and THEN followed by commands. For example, IF TEMP>30 THEN ledcolor red turns the LED red when temperature exceeds 30.
    Use automationadd followed by a rule. For example, automationadd IF TEMP>30 THEN ledcolor red turns the LED red when temperature exceeds 30. Type automationlist to see all rules.

### Cluster 30
    Type help to see all available commands. Type help followed by a command name for detailed usage information.
    Run help to see all available commands. Run help followed by a command name for detailed usage information.
    Use help to see all available commands. Use help followed by a command name for detailed usage information.

### Cluster 31
    Type ntpsync to synchronise from an NTP server. You need WiFi first. Type time to see the current time.
    Run ntpsync to synchronise from an NTP server. You need WiFi first. Use time to see the current time.

### Cluster 32
    Type openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.
    The command is openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.

### Cluster 33
    Type status to see system status including WiFi, sensors, and uptime.
    status shows system status including WiFi, sensors, and uptime.

### Cluster 34
    BLE is Bluetooth Low Energy. HardwareOne uses it to scan for nearby devices and connect to BLE peripherals for short-range communication.
    BLE is Bluetooth Low Energy. HardwareOne uses it to scan for nearby devices and connect to peripherals for short-range wireless communication.

### Cluster 35
    Enable color mode with apdsmode color on, then type apdscolor to see red, green, blue, and clear readings.
    Enable color mode with apdsmode color on, then type apdscolor to see red, green, blue, and clear channel values.

### Cluster 36
    I only know about HardwareOne device commands. Python package management is outside my scope.
    I only know about HardwareOne device commands. Python serial programming is outside my scope.

### Cluster 37
    Run servo followed by the channel and the angle. The PCA9685 moves the servo immediately.
    Run servo followed by the channel number and angle. The servo moves to the specified position immediately.

### Cluster 38
    Type certinfo to see the subject, issuer, key type, validity dates, and days until expiry.
    Type certinfo to see the certificate subject, issuer, key type, validity dates, and days until expiry.

### Cluster 39
    Type servo followed by the channel number and angle to move the servo. Use servolist to see all channels.
    Type servo followed by the channel number and angle to move a servo. Use servolist to see configured servos, servoprofile to load a profile, and servocalibrate to adjust limits.

### Cluster 40
    Type servocalibrate followed by the channel number. Follow the prompts to set the minimum, center, and maximum positions.
    Type servocalibrate followed by the channel number. Follow the prompts to set minimum, center, and maximum pulse widths for that channel.

### Cluster 41
    Type tzoffsetminutes followed by the offset in minutes from UTC. For example, tzoffsetminutes -480 for US Pacific time.
    Type tzoffsetminutes followed by the offset in minutes from UTC. For example, tzoffsetminutes -480 for Pacific time. Then run ntpsync to update the clock.

============================================================
YES/NO SAME-BODY (verify each is correct for its question):

  YES: Yes. You can control HardwareOne through the serial command line, the web dashboard on any device with a browser, or the OLED menu and gamepad.
  NO : No. You can control HardwareOne through the serial command line, the web dashboard on any device with a browser, or the OLED menu and gamepad.

  YES: Yes. HardwareOne works without internet. It serves its own web dashboard, runs sensors, and runs the LLM entirely on the device.
  NO : No. HardwareOne works without internet. It serves its own web dashboard, runs sensors, and runs the LLM entirely on the device.

