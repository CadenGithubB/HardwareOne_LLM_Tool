ANSWER CHANGELOG — every before→after rewrite. Veto any line.

### verb→Type  (59 changes)
  - Run IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.
  + Type IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.
  - Run SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.
  + Type SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.
  - Run automationlist to see all configured automation rules and their status.
  + Type automationlist to see all configured automation rules and their status.
  - Run automationlist to see the list, then delete the rule by its number.
  + Type automationlist to see the list, then delete the rule by its number.
  - Run close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.
  + Type close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.
  - Run espnowsendfile followed by the peer MAC address and the file path.
  + Type espnowsendfile followed by the peer MAC address and the file path.
  - Run espnowstats to see the connection status and the list of paired peers.
  + Type espnowstats to see the connection status and the list of paired peers.
  - Run filedelete followed by the file path, or use the delete button in the Files tab.
  + Type filedelete followed by the file path, or use the delete button in the Files tab.
  - Run i2cscan to scan the I2C bus. The device also scans automatically at startup and loads drivers for recognised sensors.
  + Type i2cscan to scan the I2C bus. The device also scans automatically at startup and loads drivers for recognised sensors.
  - Run ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.
  + Type ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.
  - Run memsample for a memory snapshot or memreport for a full report.
  + Type memsample for a memory snapshot or memreport for a full report.
  - Run mqttPublishIMU 1 or mqttPublishThermal 1 to enable automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.
  + Type mqttPublishIMU 1 or mqttPublishThermal 1 to enable automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.
  - Run mqttSubscribeTopics followed by a comma-separated list of topics. Incoming messages appear in the serial log and can trigger automations.
  + Type mqttSubscribeTopics followed by a comma-separated list of topics. Incoming messages appear in the serial log and can trigger automations.
  - Run mqttstatus to see whether the device is connected to the broker and what topic prefix is being used.
  + Type mqttstatus to see whether the device is connected to the broker and what topic prefix is being used.
  - Run oledbrightness 255 to set maximum brightness. Make sure the OLED is initialised by navigating to the OLED menu.
  + Type oledbrightness 255 to set maximum brightness. Make sure the OLED is initialised by navigating to the OLED menu.
  - Run open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.
  + Type open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.
  - Run openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.
  + Type openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.
  - Run openrtc to start the DS3231 real-time clock, then rtcread to get the time. The RTC keeps time even when the device is powered off.
  + Type openrtc to start the DS3231 real-time clock, then rtcread to get the time. The RTC keeps time even when the device is powered off.
  - Run opentof to start the VL53L4CX sensor. It measures distance up to 6 metres.
  + Type opentof to start the VL53L4CX sensor. It measures distance up to 6 metres.
  - Run reboot to restart the device.
  + Type reboot to restart the device.
  - Run settings to see current values, or use the Settings tab in the web UI. Settings are saved as JSON on internal storage.
  + Type settings to see current values, or use the Settings tab in the web UI. Settings are saved as JSON on internal storage.
  - Run status to see system status including WiFi, sensors, and uptime.
  + Type status to see system status including WiFi, sensors, and uptime.
  - Run status to see the current firmware version, WiFi, filesystem, and memory info.
  + Type status to see the current firmware version, WiFi, filesystem, and memory info.
  - Run time to see the current device time, or ntpsync to sync from an NTP server.
  + Type time to see the current device time, or ntpsync to sync from an NTP server.
  - Run uptime to see how long the device has been running since the last reboot.
  + Type uptime to see how long the device has been running since the last reboot.
  - Run uptime to see if it recently rebooted. Check debug logs on the SD card for crash information.
  + Type uptime to see if it recently rebooted. Check debug logs on the SD card for crash information.
  - Run useradd followed by the username, password, and role.
  + Type useradd followed by the username, password, and role.
  - Run userlist to see all user accounts and their roles.
  + Type userlist to see all user accounts and their roles.
  - Run wifiadd followed by the network name and password, then openwifi to connect.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Run wifistatus to see the connection status, IP address, and signal strength.
  + Type wifistatus to see the connection status, IP address, and signal strength.
  - Run wifistatus to show the current connection status, IP address, and signal strength.
  + Type wifistatus to show the current connection status, IP address, and signal strength.
  - Use IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.
  + Type IF followed by a condition and THEN followed by a command. For example, IF TEMP>30 THEN ledcolor red.
  - Use SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.
  + Type SENSORautostart on. For example, imuautostart on starts the IMU automatically on every boot.
  - Use automationlist to see all configured automation rules and their status.
  + Type automationlist to see all configured automation rules and their status.
  - Use automationlist to see the list, then delete the rule by its number.
  + Type automationlist to see the list, then delete the rule by its number.
  - Use batterystatus to see voltage, charge level, and charging status.
  + Type batterystatus to see voltage, charge level, and charging status.
  - Use close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.
  + Type close followed by the sensor name. For example, closeimu stops the BNO055 IMU, closetof stops the distance sensor.
  - Use closewifi to disconnect from the current network.
  + Type closewifi to disconnect from the current network.
  - Use espnowsend followed by the peer MAC address and your message.
  + Type espnowsend followed by the peer MAC address and your message.
  - Use espnowsendfile followed by the peer MAC address and the file path.
  + Type espnowsendfile followed by the peer MAC address and the file path.
  - Use filedelete followed by the file path, or use the delete button in the Files tab.
  + Type filedelete followed by the file path, or use the delete button in the Files tab.
  - Use ledcolor followed by a color name or hex code. For example, ledcolor red or ledcolor FF00FF.
  + Type ledcolor followed by a color name or hex code. For example, ledcolor red or ledcolor FF00FF.
  - Use ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.
  + Type ledcolor off or ledcolor 000000 to turn off the NeoPixel LED.
  - Use mqttPublishIMU 1 or mqttPublishThermal 1 to enable automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.
  + Type mqttPublishIMU 1 or mqttPublishThermal 1 to enable automatic sensor publishing. The device publishes sensor data to configured MQTT topics at regular intervals.
  - Use mqttSubscribeTopics followed by a comma-separated list of topics. Incoming messages appear in the serial log and can trigger automations.
  + Type mqttSubscribeTopics followed by a comma-separated list of topics. Incoming messages appear in the serial log and can trigger automations.
  - Use oledbrightness followed by a value from 0 to 255.
  + Type oledbrightness followed by a value from 0 to 255.
  - Use open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.
  + Type open followed by the sensor name. For example, openimu starts the BNO055 IMU, opentof starts the distance sensor, and openthermal starts the thermal camera.
  - Use openimu to start the BNO055 IMU, then imuread to see orientation and acceleration data.
  + Type openimu to start the BNO055 IMU, then imuread to see orientation and acceleration data.
  - Use openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.
  + Type openimu to start the BNO055, then imuread to read orientation, acceleration, and gyroscope data.
  - Use openrtc to start the DS3231 real-time clock, then rtcread to get the time. The RTC keeps time even when the device is powered off.
  + Type openrtc to start the DS3231 real-time clock, then rtcread to get the time. The RTC keeps time even when the device is powered off.
  - Use opentof to start the VL53L4CX sensor. It measures distance up to 6 metres.
  + Type opentof to start the VL53L4CX sensor. It measures distance up to 6 metres.
  - Use status to see system status including WiFi, sensors, and uptime.
  + Type status to see system status including WiFi, sensors, and uptime.
  - Use time to see the current device time, or ntpsync to sync from an NTP server.
  + Type time to see the current device time, or ntpsync to sync from an NTP server.
  - Use uptime to see how long the device has been running since the last reboot.
  + Type uptime to see how long the device has been running since the last reboot.
  - Use uptime to see if it recently rebooted. Check debug logs on the SD card for crash information.
  + Type uptime to see if it recently rebooted. Check debug logs on the SD card for crash information.
  - Use userdelete followed by the username to remove their account.
  + Type userdelete followed by the username to remove their account.
  - Use wifiadd followed by the network name and password, then openwifi to connect.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Use wifiscan to see a list of nearby WiFi networks with signal strength.
  + Type wifiscan to see a list of nearby WiFi networks with signal strength.
  - Use wifistatus to show the current connection status, IP address, and signal strength.
  + Type wifistatus to show the current connection status, IP address, and signal strength.

### verb→Type + trim  (4 changes)
  - Run wifiadd followed by the network name and password, then openwifi to connect. Once connected, open the web dashboard from your phone or laptop using the device IP address.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Run wifiadd followed by the network name and password, then openwifi to connect. The device saves credentials and reconnects automatically on reboot.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Use wifiadd followed by the network name and password, then openwifi to connect. Once connected, open the web dashboard from your phone or laptop using the device IP address.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Use wifiadd followed by the network name and password, then openwifi to connect. The device saves credentials and reconnects automatically on reboot.
  + Type wifiadd followed by the network name and password, then openwifi to connect.

### trim trailing clause  (3 changes)
  - Type rtcset followed by the date and time in YYYY-MM-DD HH:MM:SS format. Use rtcsync to copy the time between the RTC module and system clock in either direction.
  + Type rtcset followed by the date and time in YYYY-MM-DD HH:MM:SS format.
  - Type wifiadd followed by the network name and password, then openwifi to connect. Once connected, open the web dashboard from your phone or laptop using the device IP address.
  + Type wifiadd followed by the network name and password, then openwifi to connect.
  - Type wifiadd followed by the network name and password, then openwifi to connect. The device saves credentials and reconnects automatically on reboot.
  + Type wifiadd followed by the network name and password, then openwifi to connect.

