Q: Is ESP-NOW the same as WiFi?
A: ESP-NOW is a direct device-to-device radio link. It connects ESP32 units to each other without any router or access point.

Q: Does ESP-NOW require a router?
A: ESP-NOW communicates directly between ESP32 devices using peer-to-peer radio. It requires only that devices are within range of each other.

Q: Is MQTT the same as WiFi?
A: MQTT is an application-layer messaging protocol that publishes and subscribes to topics through a broker. It rides on top of an active network connection.

Q: Does MQTT work without WiFi?
A: MQTT is a messaging protocol that requires an active network connection to reach its broker. The network must be established first before MQTT can connect.

Q: Does Hardware One support 5GHz WiFi?
A: Hardware One uses the ESP32-S3, which operates on the 2.4GHz band only. Connect to a 2.4GHz network.

Q: Is BLE the same as WiFi?
A: BLE is Bluetooth Low Energy, a short-range radio for communicating with nearby phones and peripherals. It works independently and does not need a network.

Q: Does BLE require a router?
A: BLE is Bluetooth Low Energy, a short-range radio that communicates directly with nearby devices. It operates independently with no infrastructure required.

Q: Is the BME280 a motion sensor?
A: The BME280 measures temperature, humidity, and barometric pressure. It is an environmental sensor only.

Q: Does the ICM sensor measure temperature?
A: The ICM-42670-P is an inertial measurement unit that tracks acceleration and angular velocity. It is a motion and orientation sensor only.

Q: Is the VL53L1X a temperature sensor?
A: The VL53L1X is a time-of-flight distance sensor that measures how far away an object is using a laser pulse. It measures distance only.

Q: Does the MLX90614 measure humidity?
A: The MLX90614 is an infrared thermometer that measures surface temperature without contact. It senses thermal radiation only.

Q: Is the APDS9960 a GPS sensor?
A: The APDS9960 detects color, ambient light, proximity, and gestures. It is an optical and gesture sensor for close-range interaction.

Q: Does the GPS module measure altitude barometrically?
A: The GPS module derives altitude from satellite signals. Its altitude readings come from satellite triangulation, which is less precise than barometric methods.

Q: Is OTA the same as the CLI?
A: OTA stands for Over-The-Air firmware update. It replaces the running firmware wirelessly through the web interface.

Q: Does OTA use Bluetooth?
A: OTA firmware updates on Hardware One are delivered over the web interface using an HTTP connection. The update file is uploaded through the browser.

Q: Is LittleFS the same as the SD card?
A: LittleFS is the internal flash filesystem built into the ESP32-S3. It stores core firmware data, web assets, and settings and is always present on the device.

Q: Can the SD card replace LittleFS?
A: LittleFS is the internal flash filesystem that stores core firmware data, web assets, and device settings. It is integral to the device and always present.

Q: Is PSRAM the same as flash storage?
A: PSRAM is high-speed volatile RAM used for runtime data, the KV cache, and model weights during inference. It is cleared when the device powers off.

Q: Does the LLM run on WiFi?
A: The on-device LLM runs entirely on the ESP32-S3 processor using PSRAM for model weights. All inference happens locally on the chip.

Q: Does Hardware One send data to the cloud for LLM inference?
A: All LLM inference is done locally on the ESP32-S3 using the on-device model loaded into PSRAM. Everything runs on the device itself.

Q: Is ESP-NOW used for internet connectivity?
A: ESP-NOW is a direct peer-to-peer radio link between nearby ESP32 devices. It is designed for local device-to-device communication only.

Q: Does bonded mode disable WiFi?
A: Bonded mode configures the device to connect only as a station to a trusted network. The access point broadcast is turned off, but station connectivity remains active.

Q: Is MQTT a hardware sensor?
A: MQTT is a lightweight publish-subscribe messaging protocol used to send and receive data through a broker. It is a software protocol, running at the application layer.

Q: Does the PCA9685 measure anything?
A: The PCA9685 is a 16-channel PWM driver that controls servo motors and other PWM devices. It is an output controller that generates signals.

Q: Is NeoPixel a sensor?
A: NeoPixel refers to addressable RGB LEDs that produce colored light. They are output display devices controlled by digital signals.

Q: Can Hardware One run without PSRAM?
A: PSRAM is required to store the LLM model weights and KV cache for on-device inference. The LLM feature depends on PSRAM being available.

Q: Is the CLI the same as the web interface?
A: The CLI is a serial command-line interface accessed over USB. It provides direct text-based interaction with the device for configuration and diagnostics.

Q: Does ESP-NOW use TCP/IP?
A: ESP-NOW uses its own lightweight frame protocol directly over the 802.11 radio. It is a low-level, low-latency link layer protocol.

Q: Is automation the same as the LLM?
A: The automation system runs user-defined rules and triggers for sensors and outputs. It executes conditional logic based on sensor readings and schedules.

Q: Does the LLM require an SD card?
A: The LLM model file can be stored on the internal LittleFS filesystem. An SD card provides additional storage space but is optional if the model fits in internal flash.

Q: Is Hardware One a cloud device?
A: Hardware One is a self-contained embedded device. All processing, inference, and storage happen locally on the ESP32-S3.

Q: Does BME280 detect motion?
A: The BME280 is an environmental sensor that measures temperature, humidity, and barometric pressure. It senses atmospheric conditions only.

Q: Is the VL53L1X affected by ambient light?
A: The VL53L1X is a time-of-flight sensor that uses a narrow 940nm laser pulse and measures return time. It is relatively immune to ambient light because it measures timing rather than reflected intensity.

Q: Does WiFi work in bonded mode?
A: In bonded mode the device connects as a station to a trusted network. Station-mode connectivity remains fully active with the access point broadcast turned off.

Q: Is ESP-NOW range the same as WiFi range?
A: ESP-NOW typically achieves roughly 10-30 meters indoors for reliable links. Range depends on environment and antenna orientation between the paired devices.

Q: Does the on-device LLM understand all languages?
A: The on-device LLM is a tiny domain-specific model trained on Hardware One Q&A data in English. It answers questions about this device in English only.

Q: Is the LLM on Hardware One the same as ChatGPT?
A: The Hardware One LLM is a tiny custom model with around 5 million parameters trained specifically on Hardware One documentation. It answers questions about this device only.
